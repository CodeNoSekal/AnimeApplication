// Isolated home regression tests; other screen tests still target obsolete APIs.
// gradlew :app:testDebugUnitTest --tests '*HomeViewModelTest' -I scripts/home-tests.init.gradle
gradle.beforeProject { project ->
    project.plugins.withId('com.android.application') {
        project.android.sourceSets.getByName('test').kotlin.setSrcDirs([
            'src/test/java/com/dmitry/yume/presentation/screens/home'
        ])
    }
}
